//////////////////////////////////////////////////////////////////////

#pragma once

#include "portmacro.h"

//////////////////////////////////////////////////////////////////////
// events returned from wifi_wait_for_event()

uint32_t constexpr wifi_event_connected = BIT0;
uint32_t constexpr wifi_event_disconnected = BIT1;

//////////////////////////////////////////////////////////////////////

esp_err_t wifi_init();
esp_err_t wifi_factory_reset();
uint32_t wifi_wait_for_event(TickType_t timeout);
int8_t wifi_get_rssi();